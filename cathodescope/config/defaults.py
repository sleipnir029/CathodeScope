"""Default configuration values for CathodeScope.

Provides default values for all configurable parameters:
fmax, max_steps, lattice_tolerance, volume_tolerance, bond length limits, etc.

Implemented in T-05.
"""
