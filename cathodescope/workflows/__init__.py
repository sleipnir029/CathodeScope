"""CathodeScope workflow orchestration layer.

Sequences tools into reproducible pipelines via WorkflowEngine.
May import from models/*, tools/*, validation/*.
Must NOT import from benchmark/*, app/*, agent/*.
Implemented in T-18 and T-19.
"""
