"""Workflow base classes.

Implements:
- WorkflowRegistry: registry of available workflow definitions.
- WorkflowDefinition: declarative step sequence with metadata.
- WorkflowContext: typed dataclass for inter-step state.

Implemented in T-18.
"""
