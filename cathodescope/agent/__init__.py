"""CathodeScope agent layer — Phase 5 placeholder.

This package is intentionally empty at Phase 4. It defines the correct
dependency boundary so Phase 5 agent development starts cleanly.

Allowed imports (Phase 5, when implemented):
    - cathodescope.models.*
    - cathodescope.workflows.engine

Forbidden imports (never):
    - cathodescope.tools.* (agents orchestrate via workflows, not tools directly)
"""

__all__: list[str] = []
