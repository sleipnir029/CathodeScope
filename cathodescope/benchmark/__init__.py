"""CathodeScope benchmark layer.

Runs the structural_analysis workflow across all benchmark materials.
Depends on models/*, workflows/*, provenance/*.
Must NOT import from tools/* directly.
Implemented in T-22 and T-23.
"""
